{
  "name": "@radar/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src scripts --ext .ts,.mjs",
    "test": "jest --passWithNoTests"
  },
  "devDependencies": {
    "@types/jest": "^29",
    "esbuild": "^0.28.1",
    "jest": "^29",
    "jest-environment-jsdom": "^29",
    "ts-jest": "^29"
  }
}
